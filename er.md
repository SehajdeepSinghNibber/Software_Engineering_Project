# Entity-Relationship Diagram

The current application database is MongoDB, using the `pingly` database. Based on the implemented Mongoose schemas, the only persisted entity is `User`.

```mermaid
erDiagram
    USER {
        ObjectId _id PK
        string email UK "required"
        string fullName "required"
        string password "required, min length 6"
        string profilePic "default empty string"
        Date createdAt
        Date updatedAt
    }
```

## Entity Details

### `User`

Stored by `server/src/models/user.model.js`.

- `_id`: MongoDB-generated primary key.
- `email`: required and unique.
- `fullName`: required.
- `password`: required, with a minimum length of six characters. The application should store this as a password hash rather than plaintext.
- `profilePic`: optional string, defaulting to an empty string.
- `createdAt` and `updatedAt`: automatically generated by Mongoose timestamps.

## Relationships

No persisted relationships are currently defined. The ML model catalog, uploaded images, dehazing requests, and inference results are handled by the inference service and API request/response flow; they do not currently have MongoDB schemas or foreign-key relationships in this codebase.

## Project Structure

The project is organized as a Next.js frontend, a Fastify API server, and a Python inference service. The following diagram shows the main ownership boundaries and dependencies.

```mermaid
flowchart TB
    subgraph FRONTEND["client/ - Next.js frontend"]
        PUBLIC["Public pages\nabout, docs, research, use-cases"]
        AUTH_UI["Auth pages\nlogin, signup"]
        APP_UI["Authenticated app\ndashboard, studio, models, settings"]
        SHELL["AppShell + SessionProvider"]
        API_CLIENT["Typed API client\nclient/src/lib/api.ts"]

        PUBLIC --> AUTH_UI
        AUTH_UI --> SHELL
        APP_UI --> SHELL
        SHELL --> API_CLIENT
    end

    subgraph SERVER["server/ - Fastify API"]
        FASTIFY["Fastify app\nserver/src/app.js"]
        AUTH_ROUTES["Auth routes\n/api/v1/auth"]
        ML_ROUTES["ML routes\n/api/v1/ml"]
        AUTH_CONTROLLER["Auth controller"]
        ML_CONTROLLER["ML controller"]
        AUTH_MIDDLEWARE["JWT auth middleware"]
        USER_MODEL["User model\nMongoose schema"]
        DB["MongoDB\npingly database"]
        ML_SERVICE["ML service adapter"]

        FASTIFY --> AUTH_ROUTES
        FASTIFY --> ML_ROUTES
        AUTH_ROUTES --> AUTH_CONTROLLER
        ML_ROUTES --> ML_CONTROLLER
        AUTH_CONTROLLER --> AUTH_MIDDLEWARE
        AUTH_CONTROLLER --> USER_MODEL
        USER_MODEL --> DB
        ML_CONTROLLER --> AUTH_MIDDLEWARE
        ML_CONTROLLER --> ML_SERVICE
    end

    subgraph INFERENCE["ML/ - Python inference service"]
        PYTHON_API["Inference HTTP API\ninference_service/app.py"]
        PREPROCESS["Preprocessing"]
        MODEL_LOADER["Model loader"]
        MODELS["Research models\nDeHamer, AOD-Net, Light-DehazeNet"]
        POSTPROCESS["Postprocessing"]

        PYTHON_API --> PREPROCESS
        PREPROCESS --> MODEL_LOADER
        MODEL_LOADER --> MODELS
        MODELS --> POSTPROCESS
    end

    API_CLIENT -->|HTTP + JWT cookie| FASTIFY
    ML_SERVICE -->|HTTP inference request| PYTHON_API
```

## Runtime Request Flow

```mermaid
sequenceDiagram
    participant Browser as Next.js frontend
    participant API as Fastify API
    participant Mongo as MongoDB
    participant ML as Python inference service

    Browser->>API: Auth request or protected API request
    API->>API: CORS, cookies, multipart, JWT validation
    API->>Mongo: Read or update User for auth operations
    Mongo-->>API: User document
    API->>ML: Send image, model, and size for dehazing
    ML-->>API: Processed image and model metadata
    API-->>Browser: JSON response
```

The ML catalog and dehazing results remain runtime data. They are not represented as database entities until corresponding persistence models are added.
