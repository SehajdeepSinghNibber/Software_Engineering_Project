# Runtime dependencies for the image-dehazing inference service.
# Python >= 3.10 recommended (developed against 3.13).
#
# For a CPU-only machine install torch from the CPU index to keep the wheel
# small, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9
Pillow>=10.0
numpy>=1.24
torch>=2.2
timm>=0.9